# DESIGN.md — 「稜馬のアトリエ」デザイン言語（v2 大改修）

本人が生成したモック（生成りの紙×紺×琥珀×朱・手書き・マスコット「うまくん」・躍動コラージュ）を全ページで再現するための共通ルール。**トップのヒーロー（Hero.astro）と Nav.astro が実装基準**＝迷ったらそこの書き方に合わせる。

## 世界観の言葉
あたたかい／親しみ／手作り／明るい昼の仕事場／ポップで元気／少し手描き／まっすぐ。
**禁止**：ダーク背景・ネオン/グラデ盛り・冷たいミニマル・薄グレー基調・明朝/セリフ・純黒 #000。

## トークン（src/styles/tokens.css 定義済み・必ず var() で使う）
- 面: `--paper #F7F1E4` / `--paper-2 #FBF7EE` / `--paper-3 #EFE7D5` / `--card #FFFDF8`
- 墨: `--ink #1B2A4A`（文字・線・濃い面）/ `--mut #6A6152`（副文）
- 芯: `--amber #E8A13A` / `--amber-d #C9871F` / `--amber-l #F6C878` … **面と線だけ**（下線・ピル・判子・紙吹雪・枠）
- 芯の文字色: `--amber-ink #8A5A10`（生成り上 約5.0:1・本文とラベル）／`--amber-head #A66A10`（約4.0:1・**24px以上の大見出しだけ**）。**琥珀を `color:` に使うときは必ずこの2つのどちらか**（`--amber-d` は 2.68:1 でAA未達）
- 熱: `--scarlet #E4572E` / `--scarlet-d #C33E1A`
- 角丸: `--r-sm 10 / --r-md 16 / --r-lg 26 / --r-pill`
- 影: `--sh-1/2/3`（紙の浮き）・動き: `--spring`（弾む）/`--ease`
- 書体: `--f-head`(Zen Kaku Gothic New 見出し900) / `--f-round`(Zen Maru 丸ゴ) / `--f-mono`(JetBrains Mono ラベル・数字) / `--f-hand`(**Yusei Magic 手書きマーカー**)

## 共通部品・作法（base.css にあり）
- `.wrap` 幅1180 / `.section` 縦リズム / `.reveal (.d1〜.d4)` スクロール登場 / `.rise-char` 一字立ち上げ
- **eyebrow**（セクションの小見出し）＝手書き赤：`<p class="eyebrow"><span class="dot"></span>01 — ラベル</p>`
- `.hand`＝手書きフォント。`.tape`＝マスキングテープ（position:absolute の親に置く）
- ボタン `.btn`（琥珀ピル・下影）/ `.btn.ghost`（下線型）

## 頻出モチーフ（ヒーローの実装からコピペ推奨）
- **判子**：朱の円に白抜き「馬」、`transform: rotate(-8〜-10deg)`、`box-shadow: inset 0 0 0 2.5px rgba(255,255,255,.4)`
- **マーカー下線**：inline SVG `<path>`（stroke=amber、stroke-width 6-9、手描き波）＋ `.draw-line` で描画アニメ
- **手書き注釈**：`.hand` + `--scarlet-d` + 小さな手描き矢印SVG（stroke 2.2、round cap）
- **紙吹雪**：13px前後の色付き小四角を数個、rotate散らし（多用しない・余白に置く）
- **画鋲メモ**：白紙 `#FFFDF6`＋radial画鋲＋rotate 2〜3deg＋`.hand`
- **濃紺カード**（ストリップ型）：bg=ink、白monoラベル、右上に琥珀の→、下辺3pxに分野色
- **白カード**：bg=card、border 2px ink または枠なし＋`--sh-2`、hoverで `translateY(-6〜8px) rotate(-1deg)`＋`--sh-3`

## マスコット「うまくん」素材（public/assets/mascot/）
`run.png`（疾走・ヒーローで使用中）/ `wave.png`（手を振る）/ `cheers.png`（ビール乾杯）/ `handoff.png` `handoff2.png`（箱を手渡す）/ `logo3d.png`（顔アイコン・ナビとカレンダー出勤マークで使用中）。
- 使うときは `filter: drop-shadow(3px 5px 5px rgba(27,42,74,.2))`
- 動きは**一つの要素に一つ**（bob or 触れたら跳ねる程度）。0.5〜0.7s ease-in-out。過剰に増やさない。
- 本人写真（イメージ）＝ `public/assets/photo/hero.jpg`

## 文章のトーン
一人称で元気に・素直に（「〜です！」「〜してます」）。**事実の捏造は絶対禁止**（経歴・受賞・数字を足さない）。既存テキストの意味を変えない。

## 品質チェック（各ページ共通）
- 横スクロールが出ない（scrollWidth ≦ viewport）
- モバイル390pxで崩れない・文字が読める（最低14px相当）
- 見出し階層が明確（eyebrow → h2.big → lead）
- `prefers-reduced-motion` を壊さない（base.cssが面倒を見る。無限アニメを新設するときだけ注意）
- 画像は必ず実在パス（works.ts のもの）。リンク切れを作らない
- 文字色のコントラストは生成り（#F7F1E4）の上で **小さい文字 4.5:1 以上／24px以上の大見出し 3:1 以上**。色付きの地に載せるときはその地で測る
- **7分野の虹は色凡例と色彩実験ゾーンだけ**。個人史の年表や「活動と挑戦」のような分野でないものに虹を使わない。色の手がかりを残したいときは**丸ドットや帯などの図形**に持たせ、**文字は紺（--ink）**にする
- **UIに絵文字を使わない**（チップ・見出し・ボタンも同じ）。アイコンは `src/components/Icon.astro` の線画SVG（24×24・stroke 1.8・色は currentColor）を使い、無ければそこに足す

## データと事実の扱い（2026-09-23 追記）
- **作品の正データは `src/data/works.ts` 1本**。作品ページ（`src/pages/works/[slug].astro`）・一覧・ヒーローの件数は全部ここから作る。
- 一時的に出さない作品は `/* */` でコメントアウトする方式。**件数を数えるときはコメントアウト分を除く**：`ls -1 dist/works | grep -v index.html | wc -l`（＝ビルド後の作品ページ数）で確認する。`grep -c 'slug:'` は無効分も数えるので使わない。
- 件数を文章に書くときは**ハードコードしない**。ヒーローの「◯作品ぜんぶ見る」は `works.length` 参照（`Hero.astro`）。README など手書きの箇所を触るときは、上のコマンドの実数と突き合わせる。
- 実績カード（about）の大きい文字は4文字以上で自動的に小さくする（`hl-n--long`／`clamp(1.4rem, 2.2vw, 1.9rem)`）。長いラベルを入れても行送りが崩れない。
- **スライド画像は横長（16:10）を既定にする**。スライド枠が横長（デスクトップ 416×260・モバイル 285×178）なので、縦長の画像をそのまま入れると左右が大きく空き、画面内の文字が読めない。PC画面・スライド資料・図はこの既定どおり横長で用意する。
- **スマホアプリの縦長スクショは切らずに `portrait: true` を付ける**（`works.ts` の `slideGroups` のオプション）。タイル枠が縦（3:4）になり、`object-fit: cover; object-position: top center` で**画面の上6割ほどを大きく**見せる（`works/[slug].astro` の `.slide--tall`）。全体はクリックしたライトボックスで原寸（縦長のまま）見られるので、画像ファイル自体を切り抜かない。
- **ライトボックスのキャプションに枚数を書かない**。`Lightbox.astro` は下部に固定のカウンター（`1 / 2`）を出すので、キャプションにも枚数を入れると長いキャプションの折り返しがカウンターに重なる。
- `approach`（工夫したこと）は1つの `<p>` で描画される＝改行は効かない。**字数の上限は summary 90字・goal 90字・approach 350字（代表作 settlo / nogi-photo / deathcamer は400字まで）**。これを超えるなら機能の列挙から削る。
- **上限を決める前に書いた作品文は順次そろえる**。2026-09-24 に上限を当てたのは長かった6作品だけで、ほかの作品（例: ポートフォリオ vol.1）はまだ超えている。次にその作品を触るときに90字・350字へそろえる。
- **summary の第1文は全角30字前後で言い切る**。一覧カード（`WorkCard.astro`）は summary を2行で切る（`-webkit-line-clamp: 2`）ため、1280幅では先頭32字ぶんしか出ない。第1文が長いと語の途中で切れる。
- 作品文に書く事実（受賞名・体制・機能・数値）は、必ず出典（各プロジェクトの README・ヘルプ画面・自分で残した記録）で裏づけを取る。裏づけが無いものは書かない。受賞と他の出来事を因果でつなげない。
